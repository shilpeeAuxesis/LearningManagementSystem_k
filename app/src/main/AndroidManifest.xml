<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.learningmanagement">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".parent_account.activity.ForgetPasswordParentActivity"></activity>
        <activity android:name=".parent_account.activity.RegisterParentActivity" />
        <activity android:name=".ui.activity.ForgetPasswordTeacherActivity" />
        <activity
            android:name=".ui.activity.TeacherDashboardActivity"
            android:label="@string/title_activity_teacher_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.activity.TeacherRegisterActivity" />
        <activity android:name=".parent_account.activity.LoginParentActivity" />
        <activity android:name=".ui.activity.TeacherLoginActivity" />
        <activity android:name=".ui.activity.LoginActivity" />
        <activity android:name=".ui.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".parent_account.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>